# flask-demo-app
Flask based application that uses FinnKino and Leffatykki API

# Installation

* `git clone <this repo>`
* `pip install -r requierments.txt`

# Starting the server

* `python app.py`
